# The application.
anthropic>=0.70      # extraction via the Messages API
pymupdf>=1.24        # PDF text layer (and, later, token bounding boxes)
pytest>=8.0          # tests only; kept in the image so tests run in the container
